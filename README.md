# amp-viewer (https://lihautan.com/amp-viewer)

> This branch is used to build the `amp-viewer-host.max.js` from [amp project](https://github.com/ampproject/amphtml).

To build `amp-viewer-host.max.js`, run `yarn build`.